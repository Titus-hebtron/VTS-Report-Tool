# Database
DATABASE_URL=postgresql://user:pass@localhost:5432/patroldb

# AWS / S3 (for image storage)
AWS_ACCESS_KEY_ID=your_access_key
AWS_SECRET_ACCESS_KEY=your_secret_key
AWS_REGION=us-east-1
S3_BUCKET=your-bucket-name
S3_PREFIX=whatsapp-uploads/

# Backend
JWT_SECRET=change-me
BACKEND_BASE_URL=http://localhost:8000

# Push notifications (example Firebase server key if using FCM server sends)
FCM_SERVER_KEY=your-fcm-server-key

# ============================================
# GOOGLE DRIVE BACKUP CONFIGURATION
# ============================================
# Option 1: File path (Render Secret Files - RECOMMENDED)
# On Render: Add Secret File at /etc/secrets/google_service_account.json
# Then set this env var to point to it
GOOGLE_APPLICATION_CREDENTIALS=/etc/secrets/google_service_account.json

# Option 2: Inline JSON (if not using Secret Files)
# Copy entire JSON content from your service account key file
# GOOGLE_CREDENTIALS_JSON={"type":"service_account","project_id":"vts-backup-tool","private_key_id":"xxx","private_key":"-----BEGIN PRIVATE KEY-----\nxxx\n-----END PRIVATE KEY-----\n","client_email":"vts-backup-service@vts-backup-tool.iam.gserviceaccount.com","client_id":"xxx","auth_uri":"https://accounts.google.com/o/oauth2/auth","token_uri":"https://oauth2.googleapis.com/token","auth_provider_x509_cert_url":"https://www.googleapis.com/oauth2/v1/certs","client_x509_cert_url":"xxx"}

# Alternative: AWS Secrets Manager
# GOOGLE_CREDENTIALS_SECRET_NAME=vts-google-drive-credentials

# Alternative: Azure Key Vault
# AZURE_KEY_VAULT_NAME=your-vault-name
# GOOGLE_CREDENTIALS_SECRET_NAME=vts-google-drive-credentials

# ============================================
# SMTP EMAIL CONFIGURATION
# ============================================
# Option 1: Individual environment variables
SMTP_SERVER=smtp.gmail.com
SMTP_PORT=587
SMTP_USERNAME=your-email@gmail.com
SMTP_PASSWORD=your-app-password

# Option 2: JSON configuration
# SMTP_CREDENTIALS_JSON={"smtp_server":"smtp.gmail.com","smtp_port":587,"username":"your-email@gmail.com","password":"your-app-password"}

# Alternative: AWS Secrets Manager
# SMTP_CREDENTIALS_SECRET_NAME=vts-smtp-config

# Alternative: Azure Key Vault
# AZURE_KEY_VAULT_NAME=your-vault-name
# SMTP_CREDENTIALS_SECRET_NAME=vts-smtp-config